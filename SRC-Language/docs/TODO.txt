TODO:
(CANCEL)1. >>和<<是否支持连续操作，例如
	RAW msg>>U32>>",">>U32
	可以用多个变量代替这种形式
(DONE)2. simple_declare的语法需要简化和提炼
(DONE)3. 按照C语言的数值表示法，比如：123L,123LL,0123（8进制）,0xA0
(DONE)7. 支持U32和ip字符串的相互转化
(CANCEL)8. 支持结构体
(DONE)9. 支持命令里的结构体数组
(DONE)14.移植到windows下，生成dll和lib文件
(DONE)16.给TCP和UDP连接的send和recv增加超时时间
(CANCEL)17.使全局变量能使用BEGIN和END函数
(DONE)19.增加SLEEP函数，睡眠指定时间
(CANCEL)6. 在编译期检查FUN函数的参数有效性
(DONE)15.使SRC源文件可以引用命令行参数($1,$2,...)
(DONE)5. 把END求值和延后求值区分开来，END函数调用时可以直接写入数据
(DONE)20.延后求值只针对有依赖项的变量，无依赖的变量不加入延后求值
(DONE)22.使用开关选择是否打印出16进制数据，默认关闭
	增加函数DEBUG，关闭或打开调试信息
(DONE)11.长度字段为2字节，1字节的字符串
	增加STR1,STR2,STR4类型，分别表示1Byte，2Bytes，4Bytes长度字段的字符串
(DONE)23.对于数组变量声明，实现一种赋值方式
	实现了数组的{v1,v2,...}赋值语法
(DONE)24.U8[]和S8[]类型的变量，实现使用字符串赋值方式
(DONE)28.全局变量END调用求值完成后，重置状态以重复利用
(DONE)29.取命令字段的值，采用cmd.field的形式
4. 多线程、多进程并发测试
10.根据某字段的值，选择解码的分支
12.为字符串设置特定的一些合法值(regex)，并进行断言比较
13.实现RandStr函数，接受regex，产生随机字符串
18.多文件include
21.IntAssign函数需要检查数值的范围
24.常量和没有依赖的延后求值变量，是否可以合并起来
25.在接收命令里，选择打印重要的字段
26.减少yystype结构体的大小
27.分离编译时和运行时的变量，为了多线程
30.expr支持+-*/%()等基本运算符
31.U8[]和S8[]类型的变量，打印时视情况当作字符串

BUGS:
1. 由于END求值与post求值分开进行，导致post insert的字节数不会计入END求值中
2. FUN调用函数的时机，与延后求值有冲突，可能导致encrypt之后才写入某些数据
