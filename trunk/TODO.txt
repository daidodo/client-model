TODO:
(CANCEL)1. >>和<<是否支持连续操作，例如
	RAW msg>>U32>>",">>U32
	可以用多个变量代替这种形式
(DONE)2. simple_declare的语法需要简化和提炼
(DONE)3. 按照C语言的数值表示法，比如：123L,123LL,0123（8进制）,0xA0
(DONE)7. 支持U32和ip字符串的相互转化
(CANCEL)8. 支持结构体
(DONE)9. 支持命令里的结构体数组
(DONE)14.移植到windows下，生成dll和lib文件
(DONE)16.给TCP和UDP连接的send和recv增加超时时间
(CANCEL)17.使全局变量能使用BEGIN和END函数
(DONE)19.增加SLEEP函数，睡眠指定时间
(CANCEL)6. 在编译期检查FUN函数的参数有效性
4. 多线程、多进程并发测试
5. 把END求值和延后求值合并
10.根据某字段的值，选择解码的分支
11.长度字段为2字节，1字节的字符串
12.为字符串设置特定的一些合法值(regex)
13.多文件include
15.使SRC源文件可以引用命令行参数($1,$2,...)
18.IntAssign函数需要检查数值的范围
20.实现RandStr函数，接受regex，产生随机字符串

BUGS:
1. 由于END求值与post求值分开进行，导致post insert的字节数不会计入END求值中
(FIXED)2. Post求值的变量没有记录字节序
2. FUN调用函数的时机，与延后求值有冲突，可能导致encrypt之后才写入某些数据
